# Cable Simulation

Adds a Cable Simulation extension for Isaac Sim.

## Usage

1. Add the parent folder of this extension as an extension search path in Isaac Sim.
2. Enable **Cable Simulation** in `Window > Extensions`.
3. Use the Cable Simulation tools from the Isaac Sim UI to create and configure procedural cable setups.
